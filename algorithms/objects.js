// const pipeFix = (numbers) => {
//   const a = Math.min(...numbers)
//   const b = Math.max(...numbers)
//   const emptyArr = []
//   for (let i = a; i <= b; i++) {
//     emptyArr.push(i)
//   }
//   return emptyArr

// }

// // console.log(pipeFix([1,2,3]))



// // const pipeFix = numbers =>
// // Array.from({ length: numbers[numbers.length - 1] - numbers[0] + 1 },(_, i) => numbers[0] + i)

// // console.log(pipeFix(2))


// var myArr = Array.from([1]);
// console.log(myArr)